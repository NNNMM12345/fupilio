var colors = ['#1a535c', '#4ecdc4', '#f7fff7', '#ff6b6b', '#ffe66d']
var epilio = {
	e : document.getElementById("ee"),
	p : document.getElementById("pp"),
	i : document.getElementById("ii"),
	l : document.getElementById("ll"),
	i : document.getElementById("iii"),
	o : document.getElementById("oo")
};
epilio.e.style.color = String(colors[Math.floor(Math.random() * colors.length)]);
epilio.p.style.color = String(colors[Math.floor(Math.random() * colors.length)]);
epilio.i.style.color = String(colors[Math.floor(Math.random() * colors.length)]);
epilio.l.style.color = String(colors[Math.floor(Math.random() * colors.length)]);
epilio.i.style.color = String(colors[Math.floor(Math.random() * colors.length)]);
epilio.o.style.color = String(colors[Math.floor(Math.random() * colors.length)]);
